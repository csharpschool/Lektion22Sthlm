@page "/fetchdata"
@using BlazorApp22.Classes;

@code {
    RefAndOut rao = new();

    protected override async Task OnInitializedAsync()
    {
        var x = 10; // Värdet ändras efter anropet
        var y = 20; // Värdet behålls efter anropet
        var z = 30; // Värdet ändras efter anropet

        var result = rao.Calculate(ref x, y, out z);
    }

}
